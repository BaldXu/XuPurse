# 版本更新日志（Changelog）

> 记录 XuPurse 各版本变更。版本号遵循语义化版本（MAJOR.MINOR.PATCH）。
>
> 更新约定：新功能 → `feat` · 修复 → `fix` · 重构 → `refactor` · 性能 → `perf`。
> 每完成一个重要功能/修复，请在本文件顶部「最新版本」下追加条目。

---

## 未发布（Unreleased）

### v0.1.0（进行中）

**里程碑**：Flutter 三端重写 + 核心记账闭环 + 三家第三方导入 + UI 全面重构。

#### 数据与核心功能
- ✅ Flutter 三端工程（android / ios / web）+ 分层骨架（core / data / domain / state / ui）
- ✅ 接入 Riverpod、drift、fl_chart、intl、uuid、archive
- ✅ 14 张业务表 + MigrationStrategy；每账本一个库 `book-<id>`
- ✅ 记账 / 转账（含手续费）/ 调账 / 余额快照 / 资产趋势闭环
- ✅ 账户管理（三类账户、合并、历史快照、删除确认）
- ✅ 分类 / 标签 / 预算 / 多币种（内置汇率 + 手动覆盖）
- ✅ 统计（收支 / 分类占比 / 预算执行 / 趋势）+ 搜索 + 搜索结果分析
- ✅ 特色业务：借贷 / 报销 / 退款 / 分期（逻辑闭环；UI 为只读列表 + 删除）
- ✅ 一木 / 昼虎 / 钱迹 `.db` 导入：解析 + 映射 + diff + 预览 + 幂等 + 账户合并（核心价值）
- ✅ 备份导出 / 导入（JSON + 压缩）

#### UI 全面重构（2026-09-20 ~ 09-22）
- ✅ 克莱因蓝主题收敛 + 骨架屏体系 + 页面基类（进场动画 / 懒挂载）
- ✅ 逐页重构：Tab 四页 + 二级页 16 页 + 弹层七件套
- ✅ 动效打磨：XpRoute 400ms 转场、弹窗分层进入、卡片/FAB 磨砂、图标包切换（简约 / Twitter 表情）
- 🚧 阶段 3 整体验收（真机走查 + 帧率基线，见 ui-refactor-plan）

#### 未开始
- ❌ Phase 6 AI 助手 / 语音记账
- ❌ Phase 7 局域网同步
- ❌ Phase 8 i18n 中英词条 / 三端正式打包

---

## 历史阶段记录（上一代 cent-xyx）

> 以下为上一代 React 实现（cent-xyx）的关键演进，供重写参考与对标。**XuPurse 从 0 重写，不在本仓库代码中体现这些提交**，仅作为设计依据。

### 阶段摘要

| 阶段 | 内容 |
|------|------|
| 账户体系 0→1 | 三大类账户（fund / record / debt）、账户表单与列表、Bill 增加 `accountId` / `incomeAccountId` |
| 币种 | 账户币种、内置汇率表 + 手动覆盖、换算公式修复 |
| 快照机制 | BalanceSnapshot、自动快照、历史快照、快照失效 |
| 逻辑迭代 | `currentBalance` 权威值、调账账单、余额重算、剔除云同步 |
| 黄金资产 | 黄金资产类型与扩展字段 |
| 第三方适配 | 一木 → 昼虎 → 钱迹 的 SQLite 解析 + 映射 + diff 导入 |
| 账户合并 | 同名账户合并候选检测与迁移 |
| 趋势统计 | 总资产趋势、资产构成、动态 Y 轴、高级统计（收支比/储蓄率/最大回撤/波动率） |
| 预算搜索 | 预算修复、搜索模糊匹配、搜索结果分析页 |
| AI 与同步 | AI 助手、多设备局域网同步、PWA 优化 |

### 主要设计决策（重写时沿用）

1. **余额权威值**：`currentBalance` 为用户矫正后的权威值，总资产直接使用，不以账单反推。
2. **调账账单**：手动改余额自动生成 `isAdjustment=true` 账单，保留历史。
3. **快照为趋势唯一数据源**：趋势图基于快照 + 二分查找，避免 O(n²)。
4. **导入幂等**：持久化 ID 映射 + diff，重复导入不产生重复数据。
5. **导入不触发余额加减**：以第三方权威值覆盖，导入账单打来源标记，重算时跳过。

---

## 模板：新版本条目格式

```markdown
### vX.Y.Z（YYYY-MM-DD）

#### ✨ 新功能
- 描述...

#### 🐛 修复
- 描述...

#### 🔧 重构 / ⚙️ 性能
- 描述...
```
